services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: sakbol
    ports:
      - "5432:5432"
    volumes:
      - sakbol_pg:/var/lib/postgresql/data

volumes:
  sakbol_pg:
